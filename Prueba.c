//Arreglo unidinamico 
/*#include<stdio.h>
#include<stdlib.h>

void main(){
	int* A;
	A=(int* )malloc(5 * sizeof(int));
	
	if(A==NULL){
		printf("No hay memoria disponible\n");
		exit(0);
	}

}

void main(){
	int elem=5;
	char *A;
	
	A= (char* )calloc(elem,sizeof(char));
	
	if(A==NULL){
		printf("\nNo hay memoria disponible\n");
		exit(0);
	}
	
}

realloc: Permite modificar el tamaño de un bloque de memoria.
free: Liberar memoria asignada */






